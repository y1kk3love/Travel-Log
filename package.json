{
  "name": "travel-log",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "serve": "npx --yes http-server -p 8080 -c-1 .",
    "build:web": "node scripts/build-web.js"
  }
}
